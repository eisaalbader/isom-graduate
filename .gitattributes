# The planner is assembled by inlining the CSS and markup of the guide, so a
# line ending is a byte in the shipped page. Checking out CRLF on Windows and
# LF elsewhere would make one commit build two different sites. Everything
# stays LF on disk on every machine.
* text=auto eol=lf
*.png binary
*.pdf binary
*.woff2 binary
*.zip binary
